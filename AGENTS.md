# Agent notes

- Run `npm test` after reader or resolve changes.
- Readers are offline. Do not spawn Claude, Cursor, Codex, or any other CLI.
- Foreign transcripts are untrusted. Keep recovered tool calls `inert: true`.
- Never commit a real session file or home-directory path.
- Human contribution rules live in `CONTRIBUTING.md`.
